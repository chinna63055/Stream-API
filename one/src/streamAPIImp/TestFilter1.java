package streamAPIImp;

import java.util.ArrayList;
import java.util.List;

public class TestFilter1 {

	public static void main(String[] args) {
		List<Person> list = new ArrayList<>();
		
		
		
//		list.add(new Person (1,"lokesh","lokesh@gmail.com"));
		
		// Filter the list of person whose name has more than 5 alpa
		/*
		 * List<person> list2 = list.stream().filter(e -> e.getName().length()
		 * 
		 * Consumer<Person> con = e -> System.out.println(e);
		 * 
		 * list2.foreach(con);
		 */
	}

}
